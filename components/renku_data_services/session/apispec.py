# generated by datamodel-codegen:
#   filename:  api.spec.yaml
#   timestamp: 2026-07-15T18:42:14+00:00

from __future__ import annotations

from enum import StrEnum

from pydantic import AwareDatetime, ConfigDict, Field, RootModel
from renku_data_services.session.apispec_base import BaseAPISpec


class LauncherType(StrEnum):
    interactive = "interactive"
    non_interactive = "non-interactive"


class BuildPlatform(StrEnum):
    linux_amd64 = "linux/amd64"
    linux_arm64 = "linux/arm64"


class EnvironmentKind(StrEnum):
    GLOBAL = "GLOBAL"
    CUSTOM = "CUSTOM"


class EnvironmentImageSourceImage(StrEnum):
    image = "image"


class EnvironmentImageSourceBuild(StrEnum):
    build = "build"


class EnvironmentPatchCommand(RootModel[list[str] | None]):
    root: list[str] | None = Field(
        ...,
        description="The command that will be run i.e. will overwrite the image Dockerfile ENTRYPOINT, equivalent to command in Kubernetes",
        min_length=1,
    )


class EnvironmentPatchArgs(RootModel[list[str] | None]):
    root: list[str] | None = Field(
        ...,
        description="The arguments that will follow the command, i.e. will overwrite the image Dockerfile CMD, equivalent to args in Kubernetes",
        min_length=1,
    )


class Status(StrEnum):
    in_progress = "in_progress"
    failed = "failed"
    cancelled = "cancelled"


class BuildNotCompletedPart(BaseAPISpec):
    model_config = ConfigDict(
        extra="forbid",
    )
    status: Status = Field(..., examples=["in_progress"])


class Status1(StrEnum):
    succeeded = "succeeded"


class Status2(StrEnum):
    cancelled = "cancelled"


class BuildPatch(BaseAPISpec):
    model_config = ConfigDict(
        extra="forbid",
    )
    status: Status2 | None = None


class BuildLogs(RootModel[dict[str, str]]):
    root: dict[str, str]


class BuildResult(BaseAPISpec):
    model_config = ConfigDict(
        extra="forbid",
    )
    image: str = Field(
        ...,
        description="A container image",
        examples=["renku/renkulab-py:3.10-0.18.1"],
        max_length=500,
        min_length=3,
    )
    completed_at: AwareDatetime = Field(
        ...,
        description="The date and time the resource was created (in UTC and ISO-8601 format)",
        examples=["2023-11-01T17:32:28Z"],
    )
    repository_url: str
    repository_git_commit_sha: str


class BuildStatus(StrEnum):
    in_progress = "in_progress"
    succeeded = "succeeded"
    failed = "failed"
    cancelled = "cancelled"


class EnvVar(BaseAPISpec):
    name: str = Field(
        ..., examples=["MY_VAR"], max_length=256, pattern="^[a-zA-Z_][a-zA-Z0-9_]*$"
    )
    value: str | None = Field(None, max_length=500)


class Error(BaseAPISpec):
    code: int = Field(..., examples=[1404], gt=0)
    detail: str | None = Field(
        None, examples=["A more detailed optional message showing what the problem was"]
    )
    message: str = Field(
        ..., examples=["Something went wrong - please try again later"]
    )
    trace_id: str | None = Field(
        None,
        description="Sentry trace ID for linking to corresponding log entries",
        examples=["ac93950e9e114a55c67fb8e5ef519bbe"],
    )


class ErrorResponse(BaseAPISpec):
    error: Error


class GetEnvironmentParams(BaseAPISpec):
    model_config = ConfigDict(
        extra="forbid",
    )
    include_archived: bool = Field(
        False, description="Whether to return archived environments or not"
    )


class EnvironmentsGetParametersQuery(BaseAPISpec):
    get_environment_params: GetEnvironmentParams | None = None


class BuildsBuildIdLogsGetParametersQuery(BaseAPISpec):
    max_lines: int = 250


class EnvironmentWithoutContainerImage(BaseAPISpec):
    id: str = Field(
        ...,
        description="ULID identifier",
        max_length=26,
        min_length=26,
        pattern="^[0-7][0-9A-HJKMNP-TV-Z]{25}$",
    )
    name: str = Field(
        ...,
        description="Renku session name",
        examples=["My Renku Session :)"],
        max_length=99,
        min_length=1,
    )
    creation_date: AwareDatetime = Field(
        ...,
        description="The date and time the resource was created (in UTC and ISO-8601 format)",
        examples=["2023-11-01T17:32:28Z"],
    )
    description: str | None = Field(
        None, description="A description for the resource", max_length=500
    )
    default_url: str = Field(
        ...,
        description="The default path to open in a session",
        examples=["/lab"],
        max_length=200,
    )
    uid: int = Field(
        ..., description="The user ID used to run the session", gt=0, le=65535
    )
    gid: int = Field(
        ..., description="The group ID used to run the session", gt=0, le=65535
    )
    working_directory: str | None = Field(
        None,
        description="The location where the session will start, if left unset it will default to the session image working directory.",
        examples=["/home/jovyan/work"],
        min_length=1,
    )
    mount_directory: str | None = Field(
        None,
        description="The location where the persistent storage for the session will be mounted, usually it should be identical to or a parent of the working directory, if left unset will default to the working directory.",
        examples=["/home/jovyan/work"],
        min_length=1,
    )
    port: int = Field(
        ...,
        description="The TCP port (on any container in the session) where user requests will be routed to from the ingress",
        gt=0,
        lt=65400,
    )
    command: list[str] | None = Field(
        None,
        description="The command that will be run i.e. will overwrite the image Dockerfile ENTRYPOINT, equivalent to command in Kubernetes",
        min_length=1,
    )
    args: list[str] | None = Field(
        None,
        description="The arguments that will follow the command, i.e. will overwrite the image Dockerfile CMD, equivalent to args in Kubernetes",
        min_length=1,
    )
    is_archived: bool | None = Field(
        False,
        description="Whether this environment is archived and not for use in new projects or not",
    )
    strip_path_prefix: bool = Field(
        False,
        description='If set to true the default url and the base path where sessions are\nserved will be removed from all URL paths before the requests reach\nthe server running in the session. So the server in the session will\nreceive HTTP requests whose base path will be "/". However this will\nnot work unless the server running inside the session can be made\naware that paths are rewritten. For example, if the application/server\nrunning in the session serves a HTML page that then loads javascript\nand CSS, the path where these assets should be loaded from in the browser\nwill not be "/" but it has to include the prefix that was stripped. And\nthe server from the session that generated the HTML page needs to know\nwhat is the full base path (including the part that was stripped) so that\nit can make the URLs to such assets be reachable from the browser.',
    )


class Environment(EnvironmentWithoutContainerImage):
    container_image: str = Field(
        ...,
        description="A container image",
        examples=["renku/renkulab-py:3.10-0.18.1"],
        max_length=500,
        min_length=3,
    )


class EnvironmentWithImageGet(Environment):
    environment_image_source: EnvironmentImageSourceImage
    environment_kind: EnvironmentKind = EnvironmentKind.CUSTOM


class EnvironmentIdOnlyPatch(BaseAPISpec):
    id: str | None = Field(
        None,
        description="Id of the environment to use",
        examples=["01AN4Z79ZS6XX96588FDX0H099"],
        min_length=1,
    )


class EnvironmentIdOnlyPost(BaseAPISpec):
    id: str = Field(
        ...,
        description="Id of the environment to use",
        examples=["01AN4Z79ZS6XX96588FDX0H099"],
        min_length=1,
    )


class BuildParameters(BaseAPISpec):
    repository: str = Field(..., description="A git repository URL")
    platforms: list[BuildPlatform] | None = Field(
        None, description="The target runtime platforms of a session environment."
    )
    builder_variant: str = Field(
        ...,
        description="Type of virtual environment manager when building custom environments.",
        max_length=99,
        min_length=1,
    )
    frontend_variant: str = Field(
        ..., description="User's Frontend Choice.", max_length=99, min_length=1
    )
    repository_revision: str | None = Field(
        None, description="A git revision", max_length=500, min_length=1
    )
    context_dir: str | None = Field(
        None, description="The relative path to a folder", max_length=500, min_length=1
    )


class BuildParametersPatch(BaseAPISpec):
    repository: str | None = Field(None, description="A git repository URL")
    platforms: list[BuildPlatform] | None = Field(
        None, description="The target runtime platforms of a session environment."
    )
    builder_variant: str | None = Field(
        None,
        description="Type of virtual environment manager when building custom environments.",
        max_length=99,
        min_length=1,
    )
    frontend_variant: str | None = Field(
        None, description="User's Frontend Choice.", max_length=99, min_length=1
    )
    repository_revision: str | None = Field(
        None, description="A git revision", max_length=500
    )
    context_dir: str | None = Field(
        None, description="The relative path to a folder", max_length=500
    )


class CommandAndArgs(BaseAPISpec):
    command: list[str] | None = Field(
        None,
        description="The command that will be run i.e. will overwrite the image Dockerfile ENTRYPOINT, equivalent to command in Kubernetes",
        min_length=1,
    )
    args: list[str] | None = Field(
        None,
        description="The arguments that will follow the command, i.e. will overwrite the image Dockerfile CMD, equivalent to args in Kubernetes",
        min_length=1,
    )


class BuildCommonPart(BaseAPISpec):
    model_config = ConfigDict(
        extra="forbid",
    )
    id: str = Field(
        ...,
        description="ULID identifier",
        max_length=26,
        min_length=26,
        pattern="^[0-7][0-9A-HJKMNP-TV-Z]{25}$",
    )
    environment_id: str = Field(
        ...,
        description="ULID identifier",
        max_length=26,
        min_length=26,
        pattern="^[0-7][0-9A-HJKMNP-TV-Z]{25}$",
    )
    created_at: AwareDatetime = Field(
        ...,
        description="The date and time the resource was created (in UTC and ISO-8601 format)",
        examples=["2023-11-01T17:32:28Z"],
    )
    error_reason: str | None = Field(
        None,
        description="The reason why a container image build did not succeed, if available.",
        examples=["StepOutOfMemory"],
    )


class BuildCompletedPart(BaseAPISpec):
    model_config = ConfigDict(
        extra="forbid",
    )
    status: Status1 = Field(..., examples=["succeeded"])
    result: BuildResult


class EnvironmentList(RootModel[list[Environment]]):
    root: list[Environment] = Field(..., description="A list of session environments")


class EnvironmentWithBuildGet(EnvironmentWithoutContainerImage):
    container_image: str | None = Field(
        None,
        description="A container image",
        examples=["renku/renkulab-py:3.10-0.18.1"],
        max_length=500,
        min_length=3,
    )
    build_parameters: BuildParameters
    environment_image_source: EnvironmentImageSourceBuild
    environment_kind: EnvironmentKind = EnvironmentKind.CUSTOM


class EnvironmentPost(CommandAndArgs):
    name: str = Field(
        ...,
        description="Renku session name",
        examples=["My Renku Session :)"],
        max_length=99,
        min_length=1,
    )
    description: str | None = Field(
        None, description="A description for the resource", max_length=500
    )
    container_image: str = Field(
        ...,
        description="A container image",
        examples=["renku/renkulab-py:3.10-0.18.1"],
        max_length=500,
        min_length=3,
    )
    default_url: str = Field(
        "/lab",
        description="The default path to open in a session",
        examples=["/lab"],
        max_length=200,
    )
    uid: int = Field(
        1000, description="The user ID used to run the session", gt=0, le=65535
    )
    gid: int = Field(
        1000, description="The group ID used to run the session", gt=0, le=65535
    )
    working_directory: str | None = Field(
        None,
        description="The location where the session will start, if left unset it will default to the session image working directory.",
        examples=["/home/jovyan/work"],
        min_length=1,
    )
    mount_directory: str | None = Field(
        None,
        description="The location where the persistent storage for the session will be mounted, usually it should be identical to or a parent of the working directory, if left unset will default to the working directory.",
        examples=["/home/jovyan/work"],
        min_length=1,
    )
    port: int = Field(
        8080,
        description="The TCP port (on any container in the session) where user requests will be routed to from the ingress",
        gt=0,
        lt=65400,
    )
    is_archived: bool = Field(
        False,
        description="Whether this environment is archived and not for use in new projects or not",
    )
    environment_image_source: EnvironmentImageSourceImage
    strip_path_prefix: bool = Field(
        False,
        description='If set to true the default url and the base path where sessions are\nserved will be removed from all URL paths before the requests reach\nthe server running in the session. So the server in the session will\nreceive HTTP requests whose base path will be "/". However this will\nnot work unless the server running inside the session can be made\naware that paths are rewritten. For example, if the application/server\nrunning in the session serves a HTML page that then loads javascript\nand CSS, the path where these assets should be loaded from in the browser\nwill not be "/" but it has to include the prefix that was stripped. And\nthe server from the session that generated the HTML page needs to know\nwhat is the full base path (including the part that was stripped) so that\nit can make the URLs to such assets be reachable from the browser.',
    )


class EnvironmentPatch(CommandAndArgs):
    name: str | None = Field(
        None,
        description="Renku session name",
        examples=["My Renku Session :)"],
        max_length=99,
        min_length=1,
    )
    description: str | None = Field(
        None, description="A description for the resource", max_length=500
    )
    container_image: str | None = Field(
        None,
        description="A container image",
        examples=["renku/renkulab-py:3.10-0.18.1"],
        max_length=500,
        min_length=3,
    )
    default_url: str | None = Field(
        None,
        description="The default path to open in a session",
        examples=["/lab"],
        max_length=200,
    )
    uid: int | None = Field(
        None, description="The user ID used to run the session", gt=0, le=65535
    )
    gid: int | None = Field(
        None, description="The group ID used to run the session", gt=0, le=65535
    )
    working_directory: str | None = Field(None, examples=["/home/jovyan/work"])
    mount_directory: str | None = Field(None, examples=["/home/jovyan/work"])
    port: int | None = Field(
        None,
        description="The TCP port (on any container in the session) where user requests will be routed to from the ingress",
        gt=0,
        lt=65400,
    )
    is_archived: bool | None = Field(
        None,
        description="Whether this environment is archived and not for use in new projects or not",
    )
    strip_path_prefix: bool | None = Field(
        None,
        description='If set to true the default url and the base path where sessions are\nserved will be removed from all URL paths before the requests reach\nthe server running in the session. So the server in the session will\nreceive HTTP requests whose base path will be "/". However this will\nnot work unless the server running inside the session can be made\naware that paths are rewritten. For example, if the application/server\nrunning in the session serves a HTML page that then loads javascript\nand CSS, the path where these assets should be loaded from in the browser\nwill not be "/" but it has to include the prefix that was stripped. And\nthe server from the session that generated the HTML page needs to know\nwhat is the full base path (including the part that was stripped) so that\nit can make the URLs to such assets be reachable from the browser.',
    )


class BuildParametersPost(BuildParameters, CommandAndArgs):
    environment_image_source: EnvironmentImageSourceBuild


class Build1(BuildCommonPart):
    pass


class Build2(BuildNotCompletedPart, Build1):
    pass


class Build3(BuildCompletedPart, Build1):
    pass


class BuildList(RootModel[list[Build2 | Build3]]):
    root: list[Build2 | Build3] = Field(
        ..., description="A list of container image builds"
    )


class EnvironmentPostInLauncherHelper(EnvironmentPost):
    environment_kind: EnvironmentKind


class EnvironmentPatchInLauncher(EnvironmentPatch):
    environment_kind: EnvironmentKind | None = None
    environment_image_source: (
        EnvironmentImageSourceImage | EnvironmentImageSourceBuild | None
    ) = Field(None, description="Source of the environment's image")
    build_parameters: BuildParametersPatch | None = None


class SessionLauncher(BaseAPISpec):
    id: str = Field(
        ...,
        description="ULID identifier",
        max_length=26,
        min_length=26,
        pattern="^[0-7][0-9A-HJKMNP-TV-Z]{25}$",
    )
    project_id: str = Field(
        ...,
        description="ULID identifier",
        max_length=26,
        min_length=26,
        pattern="^[0-7][0-9A-HJKMNP-TV-Z]{25}$",
    )
    name: str = Field(
        ...,
        description="Renku session name",
        examples=["My Renku Session :)"],
        max_length=99,
        min_length=1,
    )
    creation_date: AwareDatetime = Field(
        ...,
        description="The date and time the resource was created (in UTC and ISO-8601 format)",
        examples=["2023-11-01T17:32:28Z"],
    )
    description: str | None = Field(
        None, description="A description for the resource", max_length=500
    )
    environment: EnvironmentWithImageGet | EnvironmentWithBuildGet
    resource_class_id: int | None = Field(
        ..., description="The identifier of a resource class"
    )
    disk_storage: int | None = Field(
        None,
        description="The size of disk storage for the session, in gigabytes",
        examples=[8],
        ge=1,
    )
    env_variables: list[EnvVar] | None = Field(
        None, description="Environment variables for the session pod", max_length=32
    )
    launcher_type: LauncherType


class SessionLauncherPost(BaseAPISpec):
    model_config = ConfigDict(
        extra="forbid",
    )
    name: str = Field(
        ...,
        description="Renku session name",
        examples=["My Renku Session :)"],
        max_length=99,
        min_length=1,
    )
    project_id: str = Field(
        ...,
        description="ULID identifier",
        max_length=26,
        min_length=26,
        pattern="^[0-7][0-9A-HJKMNP-TV-Z]{25}$",
    )
    description: str | None = Field(
        None, description="A description for the resource", max_length=500
    )
    resource_class_id: int | None = Field(
        None, description="The identifier of a resource class"
    )
    disk_storage: int | None = Field(
        None,
        description="The size of disk storage for the session, in gigabytes",
        examples=[8],
        ge=1,
    )
    env_variables: list[EnvVar] | None = Field(
        None, description="Environment variables for the session pod", max_length=32
    )
    launcher_type: LauncherType = LauncherType.interactive
    environment: (
        EnvironmentIdOnlyPost | EnvironmentPostInLauncherHelper | BuildParametersPost
    )


class SessionLauncherPatch(BaseAPISpec):
    model_config = ConfigDict(
        extra="forbid",
    )
    name: str | None = Field(
        None,
        description="Renku session name",
        examples=["My Renku Session :)"],
        max_length=99,
        min_length=1,
    )
    description: str | None = Field(
        None, description="A description for the resource", max_length=500
    )
    resource_class_id: int | None = Field(
        None, description="The identifier of a resource class"
    )
    disk_storage: int | None = Field(None, ge=1)
    env_variables: list[EnvVar] | None = Field(
        None, description="Environment variables for the session pod", max_length=32
    )
    environment: EnvironmentPatchInLauncher | EnvironmentIdOnlyPatch | None = None


class SessionLaunchersList(RootModel[list[SessionLauncher]]):
    root: list[SessionLauncher] = Field(
        ..., description="A list of Renku session launchers", min_length=0
    )
